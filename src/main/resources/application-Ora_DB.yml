controllerReturnedProfile: wartoscZPliku dev-yaml
controllerReturned: CR_nadpisany_DEV

spring:
  datasource:
    url: jdbc:oracle:thin:@localhost:1521/XE
    username: Pawel
    password: root

  jpa:
    hibernate:
      #ddl-auto - enable to generate database structure via hibernate
        #ddl-auto: update
    properties:
      hibernate:
        #default_schema: liquibaseapp -it works only for JPA, not for JDBC
  liquibase:
    #default-schema: liquibaseApp